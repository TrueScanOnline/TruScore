# ============================================================================
# BUILD iOS AND SUBMIT TO TESTFLIGHT - COPY AND PASTE THIS COMMAND
# ============================================================================

cd C:\TrueScan-FoodScanner; powershell -NoProfile -ExecutionPolicy Bypass -File scripts\buildIOSAndSubmit.ps1

# ============================================================================
# WHAT THIS SCRIPT DOES:
# ============================================================================
# 1. Pre-flight checks (Node.js, EAS CLI, authentication, Git)
# 2. Increments iOS build number to 6 in app.config.js
# 3. Git commit with descriptive message
# 4. Creates git tag (iOSBuild6_YYYYMMDD)
# 5. Builds iOS production build (for TestFlight)
# 6. Monitors build progress (up to 60 minutes)
# 7. Submits to App Store Connect automatically when build completes
# 8. Pushes all commits and tags to remote repository
# ============================================================================
#
# EXPECTED RESULT:
# - iOS build number 6 will be built
# - Build will be submitted to App Store Connect
# - Available for TestFlight testing on iPhone 11
# - All changes committed and pushed to Git
# ============================================================================













