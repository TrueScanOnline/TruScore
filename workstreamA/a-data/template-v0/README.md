# Workstream A Template Pack

This pack provides A5 v0.3 template headers for all required and optional Workstream A input files.

- Canonical load path is `input/*.csv`.
- Generated outputs are written to `output/` by scaffold runners.
- Templates are scaffolding artifacts only and do not contain governed population values.
